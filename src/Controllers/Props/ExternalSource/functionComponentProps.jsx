import React from 'react';



export default function FunctionComponentExternalPropsController(){  
 
     return(
            <React.Fragment>
                <h1> Class Component: props passed through Component </h1>
                {/* <div>
                    Personal Detail:[Render simple Number, String, Array, Object, another component]<br />
                    {name.firstName + ' ' + name.lastName} is getting {salary}
                    <div>
                        Role List: ArrayIteration
                        <ul>
                            {roles.map((val) => <li key={val}>{val}</li>)}
                        </ul>
                    </div>
                    <div>
                        Job Detail: ObjectIteration
                        <ul>
                            {Object.keys(workExp).map(
                                (key)=> <li key={key}>{key}: {workExp[`${key}`]}</li>
                            )}
                        </ul>
                    </div>
                </div> */}


        </React.Fragment>
    );
}